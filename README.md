# LT AI Coach Exporter

Source for the native DLL used by LT AI Coach. It exports game data through the
Teamfight Manager 2 mod API when requested. It does not change game state.

Build on Windows with the game mod SDK:

```powershell
.\build.ps1 -SdkDir <path-to-sdk>
```
